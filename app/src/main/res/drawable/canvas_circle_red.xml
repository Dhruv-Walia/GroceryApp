<?xml version="1.0" encoding="UTF-8"?>
<shape
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <solid
        android:color="@color/theme_color"/>

    <size
        android:width="@dimen/count"
        android:height="@dimen/count"/>

    <stroke android:width="0.5dp"
        android:color="@color/theme_color"/>
</shape>